# Asset inventory — Data & Research Editorial (2026-09-06)

File, source, rights, alt/caption and real-vs-concept status for every production visual.
No confidential or draft-protected bytes ship in rendered pages (verified in VALIDATION_REPORT.md).

## Inline (no file)

| Location | Content | Alt / caption |
| --- | --- | --- |
| Homepage hero (`src/pages/index.astro`) | Original SVG: sources/events → transform → insight | Captioned "Conceptual view of the practice — not a company system" |
| Lab features (homepage + `/projects`) | Per-status concept SVG fallback (problem → status → next test) | Captioned "Conceptual — not a product screenshot"; renders only when an entry has no real `image` |
| Prototypes (`work/prototypes/`) | Composition studies, same SVG grammar | Internal only, never routed or built |

## Raster / documents (`public/`, copied verbatim to build)

| File | Source / rights | Status |
| --- | --- | --- |
| `public/og-image.png` (1200×630, 45KB) | Generated by `work/make-og.mjs` (project-owned) | Real identity card, safe margins verified |
| `public/favicon.svg`, `favicon-32.png`, `apple-touch-icon.png` | Existing project-owned mark | Retained (identity unchanged) |
| `public/resume.pdf` | Owner-supplied (latest version UNVERIFIED — owner-gated) | Linked from nav/hero/404/footer |
| `public/certificates/datacamp-associate-data-analyst.pdf` (48KB) | Vendor PDF, credential ID verified | Public, linked from homepage + archive |
| `public/certificates/edx-harvardx-data-science.pdf` (124KB) | Vendor PDF, credential URL verified | Public, linked from homepage + archive |
| `public/certificates/finnovative-hackathon-2026.pdf` (380KB) | Organizer PDF, date 17-01-2026 verified | Public, on file (no homepage link) |

## Optimized content images (`src/assets/`, Astro pipeline)

| File | Rights | Rendered where |
| --- | --- | --- |
| `achievements/finnovative-hackathon-2026.jpg` (108KB, redacted scan) | Owner's award, public | `/certificates` archive only (width 800, lazy) — never homepage |
| `achievements/dazone-2025.jpg`, `rmit-…-2025.jpg` | Owner's awards, PUBLIC images but DRAFT records (dates unverified) | Optimized into `dist/_astro` but unlinked from every page — renders only after the records go public |

## Deliberately absent (no placeholders shipped)

Paper method figure (source unread — template omits the figure block when `cover` is missing);
public Lab graphics (zero public Lab entries — template concept fallback only);
per-article covers (optional by design); screenshots of any internal system (prohibited).
